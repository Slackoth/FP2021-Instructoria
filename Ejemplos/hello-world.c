// studio.h file contains functions such as scanf()
// and printf() to take input and display output
// respectively
#include <stdio.h>

int main() {
   // printf() displays the string inside quotation
   // also mostly used for displaying formatted output
   printf("Hello, World!\n");

   //Exit status
   return 0;
}